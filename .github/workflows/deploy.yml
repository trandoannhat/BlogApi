name: Deploy BlogApi .NET 8

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy source to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          port: ${{ secrets.PORT }}
          key: ${{ secrets.SSH_KEY }}
          source: "."
          target: "/var/www/demo/be"

      - name: Build and Run Docker
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            # Di chuyển vào thư mục dự án
            cd /var/www/demo/be/BlogApi
            
            # Xây dựng Docker Image mới
            docker build -t blog-api .

            # Dừng và xóa container cũ (Dùng -f để ép xóa ngay cả khi container đang lỗi)
            docker rm -f blog-api-cont || true

            # Chạy Container mới trên cổng 3005
            docker run -d \
              --name blog-api-cont \
              -p 3005:8080 \
              --add-host=host.docker.internal:host-gateway \
              -e ConnectionStrings__DefaultConnection="${{ secrets.DATABASE_URL }}" \
              --restart always \
              blog-api